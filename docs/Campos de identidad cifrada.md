Tu Me struct tiene:
	•	Campos de identidad cifrada (username, public_key, private_key, context_id)
	•	Un campo verbs: Verbs → que contiene las tablas “verbales” (be, do, have, etc.)
	•	Un Connection activo a la base SQLite (conn)
