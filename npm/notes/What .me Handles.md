 Core structure
	•	Defines a Me class that handles:
	•	GraphQL API calls (status, get, publicInfo, etc.).
	•	Identity and monad state management.
	•	Subscriptions for reactive state updates.
	•	Private mutation helpers (#verbMutation).
	•	A Proxy wrapper that logs every method call.

    